<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homedun.live.mapper.UserMapper">

    <resultMap id="userMap" type="com.homedun.live.domain.User">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="state" property="state"/>
        <result column="type" property="type"/>
        <result column="created" property="created"/>
        <result column="modified" property="modified"/>
    </resultMap>

    <sql id="columns">
        `username`,
        `password`,
        `phone`,
        `email`,
        `state`,
        `type`,
        `created`,
        `modified`
    </sql>

    <insert id="add" parameterType="com.homedun.live.domain.User">
        insert into `user`(<include refid="columns"/>)
        values(#{username}, #{password}, #{phone}, #{email}, #{state}, #{type}, NOW(), NOW())
    </insert>

    <select id="query" resultMap="userMap" parameterType="com.homedun.live.domain.User">
        SELECT id,<include refid="columns" />
        FROM `user`
        WHERE 1=1
        <if test="id != null">
            ANd id=#{id}
        </if>
        <if test="username != null">
            ANd username=#{username}
        </if>
        <if test="password != null">
            ANd password=#{password}
        </if>
        <if test="phone != null">
            ANd phone=#{phone}
        </if>
        <if test="email != null">
            ANd email=#{email}
        </if>
        <if test="state != null">
            ANd state=#{state}
        </if>
        <if test="type != null">
            ANd type=#{type}
        </if>
        <if test="offset != null and limit !=null" >
            limit #{offset}, #{limit}
        </if>
        <if test="offset == null or limit == null" >
            limit 0, 100
        </if>
    </select>

    <select id="count" resultType="java.lang.Long" parameterType="com.homedun.live.domain.User">
        SELECT count(1)
        FROM `user`
        WHERE 1=1
        <if test="id != null">
            ANd id=#{id}
        </if>
        <if test="username != null">
            ANd username=#{username}
        </if>
        <if test="password != null">
            ANd password=#{password}
        </if>
        <if test="phone != null">
            ANd phone=#{phone}
        </if>
        <if test="email != null">
            ANd email=#{email}
        </if>
        <if test="state != null">
            ANd state=#{state}
        </if>
        <if test="type != null">
            ANd type=#{type}
        </if>
    </select>

    <update id="update" parameterType="com.homedun.live.domain.User">
        update `user`
        SET
        modified = NOW()
        <if test="password != null">
            ,password = #{password}
        </if>
        <if test="phone != null">
            ,phone = #{phone}
        </if>
        <if test="email != null">
            ,email = #{email}
        </if>
        <if test="state != null">
            ,state = #{state}
        </if>
        <if test="type != null">
            ,type = #{type}
        </if>
        WHERE id = #{id}
    </update>

</mapper>
